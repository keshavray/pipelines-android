name: $(Build.BuildId)_$(Date:yyyy-MM-dd).$(Rev:r)

trigger: none

pr: none

pool:
  vmImage: ubuntu-latest

steps:

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      echo $PROD_STORE_PASSWORD
      echo $PROD_KEY_ALIAS
      echo $PROD_KEY_PASSWORD
  displayName: Printing vars